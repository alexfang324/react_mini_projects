import React from 'react';

//creating React Context object with optional default value. It has the <Provider /> and
// <Consumer /> components its properties that we will use to set up and retrieve the context
export default React.createContext('english');
